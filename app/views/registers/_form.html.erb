<%= form_for(register) do |f| %>
  <% if register.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(register.errors.count, "error") %> prohibited this register from being saved:</h2>

      <ul>
      <% register.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :weight %>
    <%= f.text_field :weight %>
  </div>

  <div class="field">
    <%= f.label :department_id %>
    <%= f.collection_select(:department_id, Department.all.order(:name), :id, :name) %>
  </div>

  <div class="field">
    <%= f.label :laboratory_id %>
    <%= f.collection_select(:laboratory_id, Laboratory.all.order(:name), :id, :name) %>
  </div>

  <div class="field">
    <%= f.label :residue_id %>
    <%= f.collection_select(:residue_id, Residue.all.order(:name), :id, :name) %>
  </div>

  <div class="field">
    <%= f.label :collection_id %>
    <%= f.collection_select(:collection_id, Collection.all.order(:max_value), :id, :max_value) %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
